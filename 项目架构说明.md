# StrawHut 项目架构说明

> **文档版本**: v1.0.0
> **创建日期**: 2026-05-01
> **关联文档**:
> - PRD-去中心化加密知识分享平台.md (v0.4.0)
> - 技术选型报告.md (v1.0.0)

---

## 目录

1. [架构设计总览](#1-架构设计总览)
2. [三层架构设计](#2-三层架构设计)
3. [项目目录结构](#3-项目目录结构)
4. [核心模块设计](#4-核心模块设计)
5. [文件格式规范](#5-文件格式规范)
6. [数据流设计](#6-数据流设计)
7. [安全架构设计](#7-安全架构设计)
8. [跨平台部署策略](#8-跨平台部署策略)
9. [P2P 扩展接口预留](#9-p2p-扩展接口预留)
10. [非功能性需求设计](#10-非功能性需求设计)
11. [错误处理与容错设计](#11-错误处理与容错设计)
12. [架构约束与最佳实践](#12-架构约束与最佳实践)

---

## 1 架构设计总览

### 1.1 架构原则

StrawHut 采用 **三层架构** 设计，遵循以下核心原则：

| 原则 | 说明 |
|------|------|
| **纯本地运行** | 所有功能在用户设备上执行，无任何网络请求模块 |
| **零持久化存储** | 软件不保存任何卡片、密钥或草稿记录，关闭后内存完全清空 |
| **关注点分离** | UI 层、业务逻辑层、数据访问层严格分离 |
| **依赖倒置** | 上层依赖抽象接口，不依赖具体实现 |
| **安全优先** | 敏感数据在内存中使用后立即清理 |
| **可扩展性** | 预留 P2P 扩展接口，支持未来平滑升级 |

### 1.2 架构全景图

```
+====================================================================================================+
|                                          StrawHut 系统架构                                          |
+====================================================================================================+
|                                                                                                    |
|  +----------------------------------------------------------------------------------------------+  |
|  |                               应用层 (Application Layer)                                     |  |
|  |  +----------------+  +------------------+  +-------------------+  +------------------------+  |  |
|  |  |  首页/导航模块  |  |  知识编辑页面     |  |  知识阅读页面      |  |  发布/解密对话框        |  |  |
|  |  | HomeScreen     |  | EditorScreen     |  | ReaderScreen      |  |  PublishDecryptDialog  |  |  |
|  |  +----------------+  +------------------+  +-------------------+  +------------------------+  |  |
|  +----------------------------------------------------------------------------------------------+  |
|                                              |                                                     |
|                                              v                                                     |
|  +----------------------------------------------------------------------------------------------+  |
|  |                              核心服务层 (Core Service Layer)                                 |  |
|  |                                                                                              |  |
|  |  +------------------+  +------------------+  +--------------------+  +---------------------+  |  |
|  |  | 加密/解密服务     |  | 文件 I/O 服务     |  | 内存草稿管理服务    |  | 密钥管理服务         |  |  |
|  |  | CryptoService    |  | FileIOService    |  | DraftManager      |  | KeyService          |  |  |
|  |  +------------------+  +------------------+  +--------------------+  +---------------------+  |  |
|  |  +------------------+  +------------------+  +--------------------+                         |  |
|  |  | 格式验证服务      |  | 完整性校验服务   |  | P2P 接口预留层     |                         |  |
|  |  | FormatValidator  |  | IntegrityService |  | P2PInterface      |                         |  |
|  |  +------------------+  +------------------+  +--------------------+                         |  |
|  +----------------------------------------------------------------------------------------------+  |
|                                              |                                                     |
|                                              v                                                     |
|  +----------------------------------------------------------------------------------------------+  |
|  |                                  数据层 (Data Layer)                                         |  |
|  |  +--------------------------------------------------------------------------+                |  |
|  |  | 文件系统 (File System)                                                   |                |  |
|  |  | - .straw 文件读写                                                        |                |  |
|  |  | - .key 文件读写                                                          |                |  |
|  |  +--------------------------------------------------------------------------+                |  |
|  +----------------------------------------------------------------------------------------------+  |
|                                                                                                    |
+====================================================================================================+
```

### 1.3 模块交互关系

```
  用户操作
     |
     v
[应用层 UI] ──────> 触发用户事件（编辑/发布/解密/打开文件）
     |
     v
[核心服务层] ────> 调用对应的业务服务（加密/解密/文件操作/草稿管理）
     |
     v
[数据层] ────────> 通过文件系统执行实际的 I/O 操作
     |
     v
  返回结果
     |
     v
[核心服务层] ────> 处理结果，更新状态
     |
     v
[应用层 UI] ──────> 渲染更新后的界面
```

---

## 2 三层架构设计

### 2.1 应用层 (Application Layer)

**职责：**
- 用户界面展示与交互
- 用户输入捕获与验证
- 调用核心服务层执行业务逻辑
- 根据服务层返回结果更新 UI 状态

**组件：**

| 组件 | 职责 | 对应页面 |
|------|------|---------|
| HomeScreen | 应用入口，提供编辑/打开/拖拽操作入口 | 首页 |
| EditorScreen | 富文本编辑器界面，工具栏，发布按钮 | 知识编辑页 |
| ReaderScreen | 解密后富文本渲染展示，元数据预览 | 知识阅读页 |
| PublishDialog | 发布/加密对话框：填写元信息、匿名切换、密钥展示、导出选项 | 发布弹窗 |
| DecryptDialog | 解密对话框：密钥输入、.key 文件上传、解密进度 | 解密弹窗 |
| AppShell | 应用外壳：导航、主题、国际化 | 全局 |

**技术要点：**
- 使用 Riverpod 进行状态管理，UI 组件通过 `ConsumerWidget` 或 `Consumer` 订阅状态
- UI 组件保持纯粹，不包含业务逻辑
- 所有异步操作通过 `FutureProvider` 或 `AsyncNotifierProvider` 处理

### 2.2 核心服务层 (Core Service Layer)

**职责：**
- 封装所有业务逻辑
- 提供清晰的服务接口供应用层调用
- 处理数据转换和格式验证
- 管理内存中的运行时状态

**服务定义：**

```dart
/// 加密服务接口
abstract class ICryptoService {
  /// 生成加密密钥（CSPRNG）
  Future<GeneratedKey> generateKey();

  /// 加密知识内容（AES-256-GCM）
  Future<EncryptedContent> encryptContent({
    required String deltaJson,
    required Uint8List key,
  });

  /// 解密知识内容（AES-256-GCM）
  Future<String> decryptContent({
    required String encryptedDataBase64,
    required String ivBase64,
    required Uint8List key,
  });

  /// 清理敏感数据（内存安全）
  void clearSensitiveData();
}

/// 文件 I/O 服务接口
abstract class IFileIOService {
  /// 读取 .straw 文件
  Future<StrawFile> readStrawFile(String filePath);

  /// 写入 .straw 文件
  Future<void> writeStrawFile({
    required String content,
    required String targetPath,
  });

  /// 读取 .key 文件
  Future<KeyFile> readKeyFile(String filePath);

  /// 写入 .key 文件
  Future<void> writeKeyFile({
    required String content,
    required String targetPath,
  });

  /// 验证文件扩展名
  bool isValidStrawFile(String filePath);
  bool isValidKeyFile(String filePath);
}

/// 内存草稿管理服务接口
abstract class IDraftManager {
  /// 保存内容到内存草稿
  void saveToDraft(String deltaJson);

  /// 从内存草稿读取内容
  String? loadFromDraft();

  /// 清除内存草稿
  void clearDraft();

  /// 检查是否存在草稿
  bool hasDraft();
}

/// 完整性校验服务接口
abstract class IIntegrityService {
  /// 计算文件内容哈希
  String computeHash(String content);

  /// 验证文件完整性
  bool verifyIntegrity({
    required String content,
    required String expectedHash,
  });
}

/// 格式验证服务接口
abstract class IFormatValidator {
  /// 验证 .straw 文件格式
  ValidationResult validateStrawFormat(Map<String, dynamic> json);

  /// 验证 .key 文件格式
  ValidationResult validateKeyFormat(Map<String, dynamic> json);
}
```

### 2.3 数据层 (Data Layer)

**职责：**
- 封装文件系统操作
- 提供统一的文件读写接口
- 处理跨平台文件路径差异

**设计要点：**

| 要点 | 说明 |
|------|------|
| 无数据库 | 不使用任何本地数据库，仅使用文件系统 |
| 无缓存 | 不缓存任何文件内容，每次操作直接读取文件系统 |
| 跨平台适配 | 通过 `path_provider` 和 `file_selector` 处理平台差异 |
| 异常处理 | 文件不存在、权限不足、格式错误等异常统一处理 |

---

## 3 项目目录结构

### 3.1 完整目录结构

```
strawhut/
│
├── pubspec.yaml                    # 项目依赖配置
├── pubspec.lock                    # 依赖锁定文件
├── analysis_options.yaml           # Dart 代码规范配置
│
├── lib/
│   ├── main.dart                   # 应用入口
│   │
│   ├── app/                        # 应用配置
│   │   ├── app.dart                # MaterialApp 配置
│   │   ├── routes.dart             # 路由定义 (go_router)
│   │   └── theme.dart              # 主题配置 (亮色/暗色)
│   │
│   ├── core/                       # 核心服务层
│   │   ├── crypto/                 # 加密模块
│   │   │   ├── crypto_service.dart           # 加密服务实现
│   │   │   ├── crypto_models.dart            # 加密相关数据模型
│   │   │   │   ├── generated_key.dart        # 生成的密钥模型
│   │   │   │   ├── encrypted_content.dart    # 加密内容模型
│   │   │   │   └── decryption_result.dart    # 解密结果模型
│   │   │   └── crypto_constants.dart         # 加密常量（算法标识等）
│   │   │
│   │   ├── file_io/                # 文件 I/O 模块
│   │   │   ├── file_io_service.dart          # 文件服务实现
│   │   │   └── file_extensions.dart          # 文件扩展名常量
│   │   │
│   │   ├── draft/                  # 内存草稿模块
│   │   │   ├── draft_manager.dart            # 草稿管理实现
│   │   │   └── draft_models.dart             # 草稿数据模型
│   │   │
│   │   ├── integrity/              # 完整性校验模块
│   │   │   ├── integrity_service.dart        # 完整性校验实现
│   │   │   └── integrity_constants.dart      # 校验算法常量
│   │   │
│   │   ├── validation/             # 格式验证模块
│   │   │   ├── format_validator.dart         # 格式验证实现
│   │   │   └── validation_result.dart        # 验证结果模型
│   │   │
│   │   ├── errors/                 # 错误定义
│   │   │   ├── strawhut_exception.dart       # 自定义异常基类
│   │   │   ├── crypto_exception.dart         # 加密相关异常
│   │   │   ├── file_exception.dart           # 文件相关异常
│   │   │   └── format_exception.dart         # 格式相关异常
│   │   │
│   │   └── utils/                  # 工具函数
│   │       ├── base64_utils.dart             # Base64 编解码工具
│   │       ├── memory_utils.dart             # 内存清理工具
│   │       └── date_utils.dart               # 日期格式化工具
│   │
│   ├── data/                       # 数据层
│   │   ├── models/                 # 数据模型
│   │   │   ├── straw_file.dart               # .straw 文件模型
│   │   │   ├── key_file.dart                 # .key 文件模型
│   │   │   ├── card_meta.dart                # 卡片元数据模型
│   │   │   └── format_version.dart           # 版本模型
│   │   │
│   │   └── repositories/           # 数据访问
│   │       └── file_repository.dart          # 文件仓库（封装文件操作）
│   │
│   ├── presentation/               # 应用层（UI）
│   │   ├── screens/                # 页面
│   │   │   ├── home/
│   │   │   │   ├── home_screen.dart          # 首页
│   │   │   │   └── widgets/
│   │   │   │       ├── action_buttons.dart   # 操作按钮组
│   │   │   │       └── drop_zone.dart        # 拖拽区域（桌面端）
│   │   │   │
│   │   │   ├── editor/
│   │   │   │   ├── editor_screen.dart        # 编辑器页面
│   │   │   │   └── widgets/
│   │   │   │       ├── quill_toolbar.dart    # 自定义工具栏
│   │   │   │       ├── quill_editor.dart     # 编辑器封装
│   │   │   │       └── preview_panel.dart    # 预览面板
│   │   │   │
│   │   │   └── reader/
│   │   │       ├── reader_screen.dart        # 阅读器页面
│   │   │       └── widgets/
│   │   │           ├── meta_preview.dart     # 元数据预览
│   │   │           └── quill_viewer.dart     # 只读渲染器
│   │   │
│   │   ├── dialogs/                # 对话框
│   │   │   ├── publish_dialog/
│   │   │   │   ├── publish_dialog.dart       # 发布对话框
│   │   │   │   └── widgets/
│   │   │   │       ├── meta_form.dart        # 元信息表单
│   │   │   │       ├── key_display.dart      # 密钥展示
│   │   │   │       └── export_options.dart   # 导出选项
│   │   │   │
│   │   │   └── decrypt_dialog/
│   │   │       ├── decrypt_dialog.dart       # 解密对话框
│   │   │       └── widgets/
│   │   │           ├── key_input.dart        # 密钥输入框
│   │   │           └── key_file_upload.dart  # 密钥文件上传
│   │   │
│   │   ├── providers/              # Riverpod 状态管理
│   │   │   ├── editor_provider.dart          # 编辑器状态
│   │   │   ├── crypto_provider.dart          # 加密状态
│   │   │   ├── card_provider.dart            # 卡片状态
│   │   │   ├── theme_provider.dart           # 主题状态
│   │   │   └── locale_provider.dart          # 语言状态
│   │   │
│   │   └── widgets/                # 公共组件
│   │       ├── app_bar.dart                  # 通用导航栏
│   │       ├── loading_indicator.dart        # 加载指示器
│   │       ├── error_banner.dart             # 错误提示横幅
│   │       └── platform_adaptive_button.dart # 平台自适应按钮
│   │
│   ├── l10n/                       # 国际化
│   │   ├── arb/
│   │   │   ├── app_zh.arb                  # 中文
│   │   │   └── app_en.arb                  # 英文（预留）
│   │   └── l10n.dart                       # 国际化配置
│   │
│   └── p2p/                        # P2P 扩展预留模块（当前未实现）
│       ├── p2p_interface.dart              # P2P 接口定义
│       ├── p2p_models.dart                 # P2P 数据模型
│       └── p2p_stub.dart                   # 空实现（占位）
│
├── test/                           # 单元测试
│   ├── core/
│   │   ├── crypto/
│   │   │   └── crypto_service_test.dart
│   │   ├── integrity/
│   │   │   └── integrity_service_test.dart
│   │   ├── validation/
│   │   │   └── format_validator_test.dart
│   │   └── draft/
│   │       └── draft_manager_test.dart
│   ├── data/
│   │   └── models/
│   │       ├── straw_file_test.dart
│   │       └── key_file_test.dart
│   └── helpers/
│       └── test_utils.dart
│
├── integration_test/               # 集成测试
│   └── app_test.dart
│
├── assets/                         # 静态资源
│   ├── images/
│   ├── icons/
│   └── fonts/
│
├── windows/                        # Windows 平台配置
│   ├── CMakeLists.txt
│   └── runner/
│
├── android/                        # Android 平台配置
│   ├── app/
│   │   ├── build.gradle
│   │   └── src/main/
│   │       ├── AndroidManifest.xml
│   │       └── kotlin/
│   └── build.gradle
│
├── ios/                            # iOS 平台配置
│   ├── Runner/
│   │   ├── Info.plist
│   │   └── Runner.entitlements
│   └── Podfile
│
└── .github/                        # CI/CD 配置
    └── workflows/
        └── ci.yml
```

### 3.2 目录结构说明

| 目录 | 用途 | 关键文件 |
|------|------|---------|
| `lib/core/` | 核心业务逻辑，所有加密、文件操作、草稿管理服务 | 各 Service 实现 |
| `lib/data/` | 数据模型和数据访问层 | StrawFile, KeyFile 等模型 |
| `lib/presentation/` | UI 组件、页面、状态管理 | 所有 Screen、Dialog、Provider |
| `lib/p2p/` | P2P 扩展预留（当前为空实现） | 接口定义、占位实现 |
| `lib/l10n/` | 国际化资源 | ARB 文件 |
| `test/` | 单元测试 | 各模块测试用例 |
| `integration_test/` | 集成测试 | 端到端测试 |
| `platforms/` | 各平台原生配置 | 由 Flutter 自动生成 |

### 3.3 命名规范

| 层级 | 命名规则 | 示例 |
|------|---------|------|
| 目录 | snake_case | `crypto_service/` |
| Dart 文件 | snake_case.dart | `crypto_service.dart` |
| 类名 | PascalCase | `CryptoService` |
| 变量/函数 | camelCase | `encryptContent()` |
| 常量 | UPPER_SNAKE_CASE | `AES_256_GCM_ALGORITHM` |
| 枚举 | PascalCase | `EncryptionAlgorithm` |
| Provider | camelCase + Provider | `cryptoServiceProvider` |

---

## 4 核心模块设计

### 4.1 富文本编辑器模块

**模块位置：** `lib/presentation/screens/editor/`

**架构设计：**

```
+-------------------------------------------------+
|                  EditorScreen                    |
|  +-------------------------------------------+  |
|  |              QuillToolbar                  |  |  <- 自定义工具栏
|  |  [B] [I] [U] [S] [H1] [H2] [List] [Code]  |  |
|  +-------------------------------------------+  |
|  |                                             |  |
|  |              QuillEditor                     |  |  <- flutter_quill 编辑器
|  |                                             |  |
|  |           (内存草稿实时保存)                   |  |
|  |                                             |  |
|  +-------------------------------------------+  |
|  |           [预览]        [发布/加密]          |  |  <- 底部操作栏
|  +-------------------------------------------+  |
+-------------------------------------------------+
```

**核心职责：**

| 功能 | 实现方式 |
|------|---------|
| 富文本编辑 | flutter_quill QuillEditor + QuillController |
| 工具栏 | flutter_quill QuillToolbar 自定义配置 |
| 内存草稿 | DraftManager 监听编辑变化，实时保存 Delta JSON |
| 预览模式 | 只读 QuillEditor 渲染 |
| 发布触发 | 调用 CryptoProvider 启动加密流程 |

**数据流：**

```
用户输入 → QuillEditor.onChanged → Delta 对象 → JSON 序列化
                                            ↓
                                    DraftManager.saveToDraft()
                                            ↓
                                    内存中保存（关闭后清空）
```

**状态管理：**

```dart
// Editor State
@Riverpod(keepAlive: false)
class EditorContent extends _$EditorContent {
  QuillController _controller = QuillController.basic();

  @override
  String build() {
    // 返回当前编辑器内容的 Delta JSON 字符串
    return jsonEncode(_controller.document.toDelta().toJson());
  }

  void updateContent() {
    // 通知状态更新，同时保存到草稿
    state = jsonEncode(_controller.document.toDelta().toJson());
    ref.read(draftManagerProvider).saveToDraft(state);
  }

  void loadFromDraft() {
    final draft = ref.read(draftManagerProvider).loadFromDraft();
    if (draft != null) {
      _controller.document = Document.fromJson(jsonDecode(draft));
      state = draft;
    }
  }

  void clear() {
    _controller = QuillController.basic();
    state = '';
    ref.read(draftManagerProvider).clearDraft();
  }
}
```

### 4.2 加密发布模块

**模块位置：** `lib/core/crypto/` + `lib/presentation/dialogs/publish_dialog/`

**加密流程图：**

```
+------------------------------------------------------------------+
|                       加密发布流程                                 |
+------------------------------------------------------------------+
|                                                                  |
|  1. [EditorScreen] 用户点击"发布/加密"按钮                          |
|                          │                                       |
|                          ▼                                       |
|  2. [PublishDialog] 弹出发布对话框                                |
|       │                                                          |
|       ├── 自动填充当前编辑器内容标题                                |
|       ├── 用户填写：发布者代号、描述、标签                          |
|       ├── 切换匿名模式开关                                         |
|       └── 点击"生成并加密"按钮                                     |
|                          │                                       |
|                          ▼                                       |
|  3. [CryptoService.generateKey()]                                 |
|       │                                                          |
|       └── 使用 CSPRNG 生成 256 位随机密钥                          |
|       └── 返回 GeneratedKey (keyBytes, keyBase64)                |
|                          │                                       |
|                          ▼                                       |
|  4. [CryptoService.encryptContent()]                              |
|       │                                                          |
|       ├── 输入: Delta JSON 字符串 + keyBytes                     |
|       ├── AES-256-GCM 加密                                       |
|       └── 返回 EncryptedContent (encryptedData, iv)              |
|                          │                                       |
|                          ▼                                       |
|  5. [IntegrityService.computeHash()]                              |
|       │                                                          |
|       └── 对即将写入的 .straw 内容计算 SHA-256 哈希               |
|                          │                                       |
|                          ▼                                       |
|  6. [StrawFile.assemble()]                                        |
|       │                                                          |
|       ├── 组装完整的 .straw JSON 结构                              |
|       │   ├── format_version: "1.0.0"                            |
|       │   ├── meta: { publisher_alias, publish_date, title... }   |
|       │   ├── content: { encrypted_data, algorithm, iv }         |
|       │   └── integrity: { hash, hash_algorithm }                |
|       └── 序列化为 JSON 字符串                                     |
|                          │                                       |
|                          ▼                                       |
|  7. [PublishDialog] 展示生成的密钥                                 |
|       │                                                          |
|       ├── 显示密钥字符串（Base64 格式）                            |
|       ├── 提供"导出 .key 文件"选项                                 |
|       └── 用户确认后清除内存中的密钥                               |
|                          │                                       |
|                          ▼                                       |
|  8. [FileIOService.writeStrawFile()]                              |
|       │                                                          |
|       ├── 弹出文件保存对话框                                       |
|       ├── 用户选择保存路径                                         |
|       └── 写入 .straw 文件                                        |
|                          │                                       |
|                          ▼                                       |
|  9. [可选] [FileIOService.writeKeyFile()]                         |
|       │                                                          |
|       ├── 如果用户选择导出 .key 文件                               |
|       ├── 弹出文件保存对话框                                       |
|       └── 写入 .key 文件                                          |
|                          │                                       |
|                          ▼                                       |
|  10. [CryptoService.clearSensitiveData()]                         |
|       │                                                          |
|       └── 清除内存中所有敏感数据（密钥、明文）                      |
|       └── 完成发布，关闭对话框                                     |
+------------------------------------------------------------------+
```

**加密服务实现：**

```dart
class CryptoService implements ICryptoService {
  final IIntegrityService _integrityService;

  CryptoService(this._integrityService);

  @override
  Future<GeneratedKey> generateKey() async {
    // 使用 CSPRNG 生成 32 字节随机密钥
    final keyBytes = Uint8List.fromList(
      List.generate(32, (_) => Random.secure().nextInt(256)),
    );
    final keyBase64 = base64Encode(keyBytes);
    return GeneratedKey(bytes: keyBytes, base64: keyBase64);
  }

  @override
  Future<EncryptedContent> encryptContent({
    required String deltaJson,
    required Uint8List key,
  }) async {
    // AES-256-GCM 加密
    final encrypter = Encrypter(AES(key, mode: AESMode.gcm));
    final iv = IV.fromSecureRandom(16);  // 16 字节 IV

    final encrypted = encrypter.encrypt(deltaJson, iv: iv);

    return EncryptedContent(
      encryptedDataBase64: base64Encode(encrypted.bytes),
      ivBase64: base64Encode(iv.bytes),
      algorithm: 'AES-256-GCM',
    );
  }

  @override
  Future<String> decryptContent({
    required String encryptedDataBase64,
    required String ivBase64,
    required Uint8List key,
  }) async {
    final encrypter = Encrypter(AES(key, mode: AESMode.gcm));
    final iv = IV(base64Decode(ivBase64));
    final encryptedBytes = base64Decode(encryptedDataBase64);

    return encrypter.decrypt(Encrypted(encryptedBytes), iv: iv);
  }

  @override
  void clearSensitiveData() {
    // Dart 中通过置零敏感数据降低内存泄露风险
    // 注意：由于 Dart GC 机制，无法保证立即清除
    // 最佳实践是尽快将引用置 null，提示 GC 回收
    _sensitiveKeyBuffer = null;
    _sensitivePlaintext = null;
  }
}
```

### 4.3 解密读取模块

**模块位置：** `lib/core/crypto/` + `lib/presentation/screens/reader/` + `lib/presentation/dialogs/decrypt_dialog/`

**解密流程图：**

```
+------------------------------------------------------------------+
|                       解密读取流程                                 |
+------------------------------------------------------------------+
|                                                                  |
|  1. [HomeScreen / EditorScreen] 用户触发打开 .straw 文件           |
|                          │                                       |
|                          ▼                                       |
|  2. [FileIOService.readStrawFile()]                               |
|       │                                                          |
|       ├── 文件选择器选择 / 拖拽导入                                |
|       ├── 读取文件内容（JSON 字符串）                              |
|       └── 返回 StrawFile 对象                                     |
|                          │                                       |
|                          ▼                                       |
|  3. [FormatValidator.validateStrawFormat()]                       |
|       │                                                          |
|       ├── 验证 JSON 结构完整性                                     |
|       ├── 验证必填字段是否存在                                     |
|       ├── 验证 format_version 兼容性                               |
|       └── 验证失败 → 返回错误提示                                   |
|                          │                                       |
|                          ▼                                       |
|  4. [ReaderScreen] 展示卡片元信息预览                              |
|       │                                                          |
|       ├── 展示标题、描述、发布者代号、标签                          |
|       ├── 展示加密算法标识                                         |
|       └── 弹出解密对话框（自动触发）                                |
|                          │                                       |
|                          ▼                                       |
|  5. [DecryptDialog] 用户选择解密方式                               |
|       │                                                          |
|       ├── 方式 A: 手动输入密钥字符串                                |
|       │   └── KeyInput 文本输入框                                  |
|       │                                                          |
|       └── 方式 B: 上传 .key 文件                                   |
|           └── FilePicker 选择 .key 文件                            |
|           └── [FileIOService.readKeyFile()] 解析密钥               |
|                          │                                       |
|                          ▼                                       |
|  6. [CryptoService.decryptContent()]                              |
|       │                                                          |
|       ├── 输入: encrypted_data + iv + key                        |
|       ├── AES-256-GCM 解密                                       |
|       └── 解密失败 → 抛出 CryptoException                         |
|       └── 解密成功 → 返回 Delta JSON 字符串                        |
|                          │                                       |
|                          ▼                                       |
|  7. [IntegrityService.verifyIntegrity()]                          |
|       │                                                          |
|       ├── 重新计算文件内容的 SHA-256 哈希                          |
|       ├── 与 integrity.hash 比对                                   |
|       ├── 校验失败 → 提示"文件已被篡改"                            |
|       └── 校验通过 → 继续                                          |
|                          │                                       |
|                          ▼                                       |
|  8. [ReaderScreen] 渲染富文本内容                                  |
|       │                                                          |
|       ├── Delta JSON → Quill Document                           |
|       ├── 只读 QuillEditor 渲染                                   |
|       └── 用户阅读知识内容                                        |
|                          │                                       |
|                          ▼                                       |
|  9. [CryptoService.clearSensitiveData()]                          |
|       │                                                          |
|       └── 清除内存中的密钥数据                                     |
+------------------------------------------------------------------+
```

**阅读器状态管理：**

```dart
// Reader State
@riverpod
class ReaderState extends _$ReaderState {
  @override
  AsyncValue<ReaderContent> build() {
    return const AsyncValue.loading();
  }

  Future<void> loadFile(String filePath) async {
    state = const AsyncValue.loading();
    try {
      final file = await ref.read(fileIOServiceProvider).readStrawFile(filePath);
      state = AsyncValue.data(ReaderContent.metaOnly(file));
    } catch (e, st) {
      state = AsyncValue.error(e, st);
    }
  }

  Future<void> decrypt(String keyInput) async {
    state = const AsyncValue.loading();
    try {
      final currentContent = state.valueOrNull;
      if (currentContent == null) return;

      final keyBytes = base64Decode(keyInput);
      final decryptedDelta = await ref.read(cryptoServiceProvider).decryptContent(
        encryptedDataBase64: currentContent.file.content.encryptedDataBase64,
        ivBase64: currentContent.file.content.ivBase64,
        key: keyBytes,
      );

      // 完整性校验
      final isValid = ref.read(integrityServiceProvider).verifyIntegrity(
        content: jsonEncode(currentContent.file.toJson()),
        expectedHash: currentContent.file.integrity.hash,
      );

      if (!isValid) {
        throw FormatException('文件完整性校验失败，文件可能已被篡改');
      }

      state = AsyncValue.data(currentContent.withDecryptedContent(decryptedDelta));
    } catch (e, st) {
      state = AsyncValue.error(e, st);
    } finally {
      ref.read(cryptoServiceProvider).clearSensitiveData();
    }
  }
}
```

### 4.4 内存草稿模块

**模块位置：** `lib/core/draft/`

**设计要点：**

| 要点 | 说明 |
|------|------|
| 纯内存存储 | 草稿仅保存在运行内存中，不写入磁盘 |
| 关闭即消失 | 软件关闭后草稿自动清空（无需手动清理） |
| 单次会话有效 | 草稿只在当前应用生命周期内有效 |
| 可主动清除 | 用户发布后可选择清除草稿 |

**实现：**

```dart
class DraftManager implements IDraftManager {
  String? _draftContent;  // 仅保存在内存中

  @override
  void saveToDraft(String deltaJson) {
    _draftContent = deltaJson;
  }

  @override
  String? loadFromDraft() {
    return _draftContent;
  }

  @override
  void clearDraft() {
    _draftContent = null;  // 释放引用，等待 GC
  }

  @override
  bool hasDraft() {
    return _draftContent != null && _draftContent!.isNotEmpty;
  }
}
```

---

## 5 文件格式规范

### 5.1 .straw 知识卡片文件格式

#### 5.1.1 文件概述

`.straw` 是 StrawHut 定义的结构化知识卡片文件格式，采用 JSON 格式封装。每个 `.straw` 文件都是一张独立的知识卡片，包含加密后的知识内容和公开可见的元数据。

#### 5.1.2 完整文件结构

```json
{
  "format_version": "1.0.0",
  "meta": {
    "publisher_alias": "Anonymous_a3f7b2c1",
    "publish_date": "2026-05-01T12:00:00Z",
    "title": "网络安全入门指南",
    "tags": ["安全", "入门", "笔记"],
    "description": "一份关于网络安全基础知识的入门笔记",
    "is_anonymous": true,
    "custom_annotations": {}
  },
  "content": {
    "encrypted_data": "Base64EncodedEncryptedContent...",
    "encryption_algorithm": "AES-256-GCM",
    "iv": "Base64EncodedIV..."
  },
  "integrity": {
    "hash": "sha256:abcdef1234567890...",
    "hash_algorithm": "SHA-256"
  }
}
```

#### 5.1.3 字段详细说明

**顶层结构：**

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `format_version` | string | 是 | 文件格式版本号，语义化版本 `major.minor.patch` |
| `meta` | object | 是 | 公开可见的元数据（未加密） |
| `content` | object | 是 | 加密后的知识内容 |
| `integrity` | object | 是 | 文件完整性校验信息 |

**meta 对象：**

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `publisher_alias` | string | 是 | 发布者代号，匿名模式下为 `Anonymous_` + 8 位随机十六进制 |
| `publish_date` | string | 是 | 发布日期，ISO 8601 格式（UTC） |
| `title` | string | 是 | 知识卡片标题 |
| `tags` | string[] | 否 | 标签列表，最多 10 个标签，每个标签最长 20 字符 |
| `description` | string | 否 | 简要描述，最长 200 字符，未解密时可见 |
| `is_anonymous` | boolean | 是 | 是否为纯匿名模式 |
| `custom_annotations` | object | 否 | 自定义键值对标注，键最长 50 字符，值最长 500 字符 |

**content 对象：**

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `encrypted_data` | string | 是 | 加密后的知识内容，Base64 编码（URL-safe） |
| `encryption_algorithm` | string | 是 | 加密算法标识，当前固定为 `AES-256-GCM` |
| `iv` | string | 是 | 初始化向量，Base64 编码（16 字节） |

**integrity 对象：**

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `hash` | string | 是 | 文件完整性校验哈希，格式为 `算法:哈希值` |
| `hash_algorithm` | string | 是 | 哈希算法标识，当前固定为 `SHA-256` |

#### 5.1.4 知识内容格式（加密前）

解密后的知识内容为 **Delta JSON 格式**（Quill 编辑器原生格式）：

```json
{
  "ops": [
    { "insert": "网络安全入门指南\n", "attributes": { "header": 1 } },
    { "insert": "这是一份关于" },
    { "insert": "网络安全", "attributes": { "bold": true } },
    { "insert": "的基础知识笔记。\n" },
    { "insert": "\n", "attributes": { "header": 2 } },
    { "insert": "基本概念\n", "attributes": { "header": 3 } },
    { "insert": "• 防火墙\n• 入侵检测\n• 加密通信\n", "attributes": { "list": "bullet" } },
    { "insert": "\n", "attributes": { "header": 3 } },
    { "insert": "代码示例：\n", "attributes": { "header": 3 } },
    { "insert": "const key = crypto.randomBytes(32);\n", "attributes": { "code-block": "javascript" } }
  ]
}
```

**Delta 格式优势：**
- 与 flutter_quill 编辑器原生兼容，无需转换
- JSON 格式可读性好，便于调试
- 支持未来扩展新操作类型
- 序列化后加密，安全性有保障

#### 5.1.5 版本兼容策略

| 版本策略 | 说明 |
|---------|------|
| **主版本号 (Major)** | 不兼容的格式变更，旧版本无法读取新版本文件 |
| **次版本号 (Minor)** | 向后兼容的功能新增，旧版本可忽略新字段读取 |
| **修订号 (Patch)** | 向后兼容的 Bug 修复 |

**兼容性处理逻辑：**

```
读取文件时：
  1. 解析 format_version
  2. 比较主版本号：
     - 相同 → 正常读取
     - 当前主版本 < 文件主版本 → 提示"需要更新软件"，拒绝读取
     - 当前主版本 > 文件主版本 → 兼容模式读取（向前兼容）
  3. 忽略未知字段（向后兼容）
```

#### 5.1.6 文件大小建议

| 内容类型 | 建议限制 | 说明 |
|---------|---------|------|
| 纯文本卡片 | 无硬性限制，建议 < 1MB | 1MB 纯文本约 50 万字 |
| 含图片卡片 | 建议 < 10MB | 图片采用 Base64 嵌入时体积膨胀约 33% |
| 超大知识库 | 建议拆分为多张卡片 | 单张卡片过大影响加密/解密性能 |

### 5.2 .key 密钥文件格式

#### 5.2.1 文件概述

`.key` 是 StrawHut 定义的密钥文件格式，用于将加密密钥独立保存为文件。`.key` 文件可与 `.straw` 文件分开传输和存储，用户通过上传 `.key` 文件实现快速解密。

**设计原则：**
- `.key` 文件独立于 `.straw` 文件，不随 `.straw` 文件一起生成
- 用户自主选择是否导出，避免密钥与加密内容一起泄露
- 采用 JSON 格式，便于解析和扩展
- 包含关联元信息，方便用户识别密钥用途

#### 5.2.2 完整文件结构

```json
{
  "format_version": "1.0.0",
  "key_metadata": {
    "key_id": "k_2026050112000000_a3f7b2c1",
    "created_at": "2026-05-01T12:00:00Z",
    "associated_card_title": "网络安全入门指南",
    "associated_card_id": null,
    "key_algorithm": "AES-256-GCM",
    "key_length_bits": 256,
    "notes": ""
  },
  "key_data": {
    "key_base64": "xK9mP2vR7wN4jQ8tL5yH3bF6dA1cE0gU6sZ9oI2eM=",
    "encoding": "base64"
  },
  "integrity": {
    "hash": "sha256:fedcba0987654321...",
    "hash_algorithm": "SHA-256"
  }
}
```

#### 5.2.3 字段详细说明

**顶层结构：**

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `format_version` | string | 是 | 文件格式版本号，与 .straw 版本独立管理 |
| `key_metadata` | object | 是 | 密钥元信息 |
| `key_data` | object | 是 | 密钥数据 |
| `integrity` | object | 是 | 文件完整性校验 |

**key_metadata 对象：**

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `key_id` | string | 是 | 密钥唯一标识，格式：`k_时间戳_随机标识` |
| `created_at` | string | 是 | 密钥创建时间，ISO 8601 格式（UTC） |
| `associated_card_title` | string | 否 | 关联的卡片标题（方便用户识别） |
| `associated_card_id` | string | 否 | 关联的卡片标识（当前版本为 null，预留） |
| `key_algorithm` | string | 是 | 密钥对应的加密算法标识 |
| `key_length_bits` | number | 是 | 密钥长度（比特） |
| `notes` | string | 否 | 用户自定义备注，最长 200 字符 |

**key_data 对象：**

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `key_base64` | string | 是 | 密钥的 Base64 编码字符串 |
| `encoding` | string | 是 | 编码方式，固定为 `base64` |

**integrity 对象：**

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `hash` | string | 是 | 文件完整性校验哈希 |
| `hash_algorithm` | string | 是 | 哈希算法标识 |

#### 5.2.4 密钥 ID 生成规则

```
格式：k_{YYYYMMDDHHmmssSSS}_{8位随机十六进制}

示例：k_20260501120000000_a3f7b2c1

说明：
  - 时间戳精确到毫秒（17 位）
  - 随机标识 8 位十六进制（32 位熵）
  - 总长度固定，便于解析
```

#### 5.2.5 安全考虑

| 安全措施 | 说明 |
|---------|------|
| 完整性校验 | `.key` 文件包含 SHA-256 哈希，防止密钥被篡改 |
| 独立存储 | 密钥文件与加密内容文件分离存储和传输 |
| 明文存储 | 密钥文件本身不加密（用户自行保管安全性） |
| 扩展名固定 | 固定使用 `.key` 扩展名，便于识别 |
| 用户提示 | 导出时明确提示用户妥善保管 `.key` 文件 |

#### 5.2.6 为什么不加密 .key 文件

| 方案 | 优点 | 缺点 | StrawHut 选择 |
|------|------|------|--------------|
| 明文 JSON | 简单、可直接使用、用户可读 | 泄露即失效 | **推荐** |
| 密码加密 | 额外安全层 | 需要用户记住密码、增加复杂度 | 不采用 |
| 自定义二进制 | 体积更小 | 可读性差、开发成本高 | 不采用 |

**理由：**
- StrawHut 的核心理念是"密钥即权限"，密钥本身需要用户妥善保管
- 增加密码保护会引入额外的密码管理复杂度，与产品定位冲突
- 用户可通过操作系统层面的文件加密（如 NTFS EFS、macOS FileVault）保护 `.key` 文件

---

## 6 数据流设计

### 6.1 加密发布数据流

```
+-----------------------------------------------------------------------------+
|                          加密发布 - 数据流                                   |
+-----------------------------------------------------------------------------+
|                                                                             |
|  [QuillEditor]                                                              |
|       │                                                                     |
|       │ Delta 对象                                                          |
|       ▼                                                                     |
|  [JSON.serialize()]                                                         |
|       │                                                                     |
|       │ Delta JSON 字符串 (明文)                                             |
|       ▼                                                                     |
|  [DraftManager.saveToDraft()] ──────────> 内存草稿 (临时保存)                 |
|       │                                                                     |
|       │ Delta JSON 字符串                                                    |
|       ▼                                                                     |
|  [CryptoService.generateKey()]                                              |
|       │                                                                     |
|       │ GeneratedKey { bytes: Uint8List(32), base64: String }               |
|       ▼                                                                     |
|  [CryptoService.encryptContent()]                                           |
|       │                                                                     |
|       │ EncryptedContent {                                                  |
|       │   encryptedDataBase64: String,                                      |
|       │   ivBase64: String,                                                 |
|       │   algorithm: "AES-256-GCM"                                          |
|       │ }                                                                   |
|       ▼                                                                     |
|  [构建 StrawFile JSON 结构]                                                  |
|       │                                                                     |
|       │ {                                                                   |
|       │   format_version,                                                   |
|       │   meta: { publisher_alias, publish_date, title, ... },              |
|       │   content: { encrypted_data, encryption_algorithm, iv },            |
|       │   integrity: { hash, hash_algorithm }                               |
|       │ }                                                                   |
|       ▼                                                                     |
|  [IntegrityService.computeHash()]                                           |
|       │                                                                     |
|       │ SHA-256 哈希值 → 写入 integrity.hash                                |
|       ▼                                                                     |
|  [JSON.serialize()] → 完整 JSON 字符串                                      |
|       │                                                                     |
|       ▼                                                                     |
|  [FileIOService.writeStrawFile()]                                           |
|       │                                                                     |
|       ▼                                                                     |
|  [文件系统] → .straw 文件                                                    |
|                                                                             |
|  同时（可选）：                                                               |
|  [构建 KeyFile JSON] → [FileIOService.writeKeyFile()] → .key 文件            |
|                                                                             |
|  最后：                                                                      |
|  [CryptoService.clearSensitiveData()] → 清除内存中的密钥和明文                |
|                                                                             |
+-----------------------------------------------------------------------------+
```

### 6.2 解密读取数据流

```
+-----------------------------------------------------------------------------+
|                          解密读取 - 数据流                                   |
+-----------------------------------------------------------------------------+
|                                                                             |
|  [文件系统] → .straw 文件                                                    |
|       │                                                                     |
|       ▼                                                                     |
|  [FileIOService.readStrawFile()]                                            |
|       │                                                                     |
|       │ 文件内容 (JSON 字符串)                                               |
|       ▼                                                                     |
|  [JSON.parse()]                                                             |
|       │                                                                     |
|       │ Map<String, dynamic>                                                |
|       ▼                                                                     |
|  [FormatValidator.validateStrawFormat()]                                     |
|       │                                                                     |
|       │ 验证通过 → StrawFile 对象                                            |
|       ▼                                                                     |
|  [展示 Meta 信息] → ReaderScreen 展示元数据预览                               |
|                                                                             |
|  用户输入密钥（手动输入 或 .key 文件上传）                                      |
|       │                                                                     |
|       ▼                                                                     |
|  [Base64.decode()] → 密钥 bytes (Uint8List)                                 |
|       │                                                                     |
|       ▼                                                                     |
|  [CryptoService.decryptContent()]                                           |
|       │                                                                     |
|       │ 输入: encryptedDataBase64 + ivBase64 + keyBytes                     |
|       │ AES-256-GCM 解密                                                     |
|       ▼                                                                     |
|  [解密结果] → Delta JSON 字符串 (明文)                                       |
|       │                                                                     |
|       ▼                                                                     |
|  [IntegrityService.verifyIntegrity()]                                       |
|       │                                                                     |
|       │ 重新计算 SHA-256 并与 integrity.hash 比对                              |
|       │ 通过 → 继续                                                          |
|       │ 失败 → 抛出异常                                                       |
|       ▼                                                                     |
|  [JSON.parse()] → Delta 对象                                                 |
|       │                                                                     |
|       ▼                                                                     |
|  [QuillEditor.readonly] → 渲染富文本内容                                      |
|                                                                             |
|  最后：                                                                      |
|  [CryptoService.clearSensitiveData()] → 清除内存中的密钥                      |
|                                                                             |
+-----------------------------------------------------------------------------+
```

### 6.3 模块间数据流总览

```
+=============================================================================+
|                        StrawHut 模块间数据流                                 |
+=============================================================================+
|                                                                             |
|   用户                                                                      |
|     │                                                                       |
|     ├── [编辑内容] ───────────────────────────────────┐                     |
|     │                                                 ▼                     |
|     │                                           DraftManager                |
|     │                                           (内存草稿)                   |
|     │                                                 │                     |
|     │                                                 ▼                     |
|     ├── [发布] ──> CryptoService ──> FileIOService ──> 文件系统 (.straw)    |
|     │                  │                   │                                |
|     │                  │                   └──> .key 文件 (可选)             |
|     │                  ▼                                                     |
|     │            IntegrityService                                            |
|     │            (完整性校验)                                                 |
|     │                                                                       |
|     ├── [打开文件] ──> FileIOService ──> FormatValidator                    |
|     │                                       │                               |
|     │                                       ▼                               |
|     │                                 ReaderScreen                          |
|     │                                 (元数据预览)                           |
|     │                                       │                               |
|     ├── [输入密钥] ──> CryptoService ──> IntegrityService                   |
|     │                  │                         │                          |
|     │                  ▼                         ▼                          |
|     │            QuillEditor (只读渲染)     [校验通过/失败]                   |
|     │                                                                       |
|     └── [上传 .key] ──> FileIOService ──> CryptoService                     |
|                                                                             |
|   内存安全边界：所有敏感数据（密钥、明文）使用后调用 clearSensitiveData()     |
|                                                                             |
+=============================================================================+
```

---

## 7 安全架构设计

### 7.1 安全设计原则

| 原则 | 说明 |
|------|------|
| **零信任本地** | 所有输入数据均视为不可信，需经过格式验证 |
| **最小权限** | 敏感数据仅在需要时存在于内存中，使用后立即清理 |
| **纵深防御** | 多层安全机制（加密 + 完整性校验 + 格式验证） |
| **安全默认** | 默认使用最强安全配置（AES-256-GCM、256 位密钥） |

### 7.2 内存安全设计

#### 7.2.1 敏感数据清单

| 数据类型 | 存在位置 | 生命周期 | 清理时机 |
|---------|---------|---------|---------|
| 加密密钥 (bytes) | CryptoService 局部变量 | 加密/解密操作期间 | 操作完成后立即置 null |
| 加密密钥 (base64) | PublishDialog UI 变量 | 对话框展示期间 | 用户确认后清除 |
| 明文内容 (Delta JSON) | EditorScreen 内存 | 编辑期间 | 发布后保留（用户可继续使用） |
| 解密后的明文 | ReaderScreen 内存 | 阅读期间 | 关闭页面后释放 |
| 内存草稿 | DraftManager._draftContent | 应用生命周期内 | 主动清除或应用退出 |

#### 7.2.2 内存清理策略

```dart
/// 内存清理工具
class MemoryUtils {
  /// 安全清除敏感数据
  /// Dart 无法直接控制内存，但可以通过以下方式降低泄露风险：
  static void secureClear(Map<String, dynamic> sensitiveMap) {
    // 1. 将所有值置为 null
    sensitiveMap.forEach((key, _) {
      sensitiveMap[key] = null;
    });
    // 2. 触发 GC 建议（非强制）
    // 注意：Dart 没有强制 GC 的 API，只能建议
  }

  /// 清除 Uint8List 中的敏感字节
  static void wipeBytes(Uint8List bytes) {
    for (var i = 0; i < bytes.length; i++) {
      bytes[i] = 0;  // 逐字节置零
    }
  }
}
```

**Dart 内存安全的局限性：**

| 限制 | 说明 | 缓解措施 |
|------|------|---------|
| GC 不可控 | Dart 使用自动垃圾回收，无法强制立即回收 | 引用置 null，降低被扫描到的概率 |
| 字符串不可变 | Dart 字符串不可变，无法原地修改 | 使用 Uint8List 存储密钥而非 String |
| 内存快照 | 系统级内存快照可能捕获敏感数据 | 应用级无法完全防护，需依赖操作系统安全 |

### 7.3 匿名发布安全设计

#### 7.3.1 匿名标识生成

```dart
/// 生成匿名标识
String generateAnonymousAlias() {
  final randomBytes = List.generate(4, (_) => Random.secure().nextInt(256));
  final hexId = randomBytes.map((b) => b.toRadixString(16).padLeft(2, '0')).join();
  return 'Anonymous_$hexId';
  // 示例：Anonymous_a3f7b2c1
}
```

#### 7.3.2 匿名模式处理逻辑

```
匿名模式开启时：
  1. publisher_alias = generateAnonymousAlias()  // 系统生成
  2. custom_annotations 中过滤可能暴露身份的信息
  3. 不收集任何设备信息、IP、时间戳（除 publish_date 外）
  4. 不记录用户行为日志

匿名模式关闭时：
  1. publisher_alias = 用户手动输入的代号
  2. 可自由填写 custom_annotations
  3. 同样不收集设备信息（纯本地运行保证）
```

### 7.4 输入安全设计

#### 7.4.1 输入验证清单

| 输入来源 | 验证项 | 处理方式 |
|---------|--------|---------|
| .straw 文件 | JSON 格式、必填字段、版本号、文件大小 | 格式错误 → 拒绝读取并提示 |
| .key 文件 | JSON 格式、必填字段、哈希校验 | 验证失败 → 拒绝使用并提示 |
| 密钥输入 | Base64 格式、长度验证（约 43 字符） | 格式错误 → 提示输入错误 |
| 元数据输入 | 标题非空、长度限制、标签数量和长度 | 超限 → 提示修改 |
| 图片 Base64 | 文件大小限制 | 超限 → 提示图片过大 |

#### 7.4.2 异常处理策略

```dart
/// 自定义异常层次
abstract class StrawHutException implements Exception {
  final String message;
  final String? code;

  const StrawHutException(this.message, {this.code});
}

class CryptoException extends StrawHutException {
  const CryptoException(super.message, {super.code});
}

class FileException extends StrawHutException {
  const FileException(super.message, {super.code});
}

class FormatException_ extends StrawHutException {
  const FormatException_(super.message, {super.code});
}

class ValidationException extends StrawHutException {
  const ValidationException(super.message, {super.code});
}
```

### 7.5 安全威胁应对矩阵

| 威胁 | 攻击方式 | StrawHut 防护措施 | 剩余风险 |
|------|---------|------------------|---------|
| 密钥泄露 | 用户分享密钥给未授权人员 | 用户教育提示，.key 文件独立存储 | 低（用户行为无法控制） |
| .key 文件泄露 | 文件被他人获取 | 独立存储、完整性校验 | 低 |
| 文件篡改 | 修改 .straw 文件内容 | SHA-256 完整性校验 + GCM Auth Tag | 极低 |
| 内存泄露 | 恶意程序扫描内存 | 使用后清除敏感数据引用 | 中（Dart GC 不可控） |
| 侧信道攻击 | 通过时序分析获取密钥 | 标准库实现，无明显时序漏洞 | 低 |
| 钓鱼文件 | 恶意构造的 .straw 文件 | 格式验证、异常结构拦截 | 低 |
| 重放攻击 | 使用旧 .straw 文件 | 无状态协议，不适用 | 不适用 |

---

## 8 跨平台部署策略

### 8.1 平台支持矩阵

| 平台 | 最低版本 | 目标版本 | 开发优先级 | 构建工具 |
|------|---------|---------|-----------|---------|
| **Windows** | Windows 10 (19041+) | Windows 11 | P0（首期） | CMake + MSVC |
| **Android** | Android 8.0 (API 26) | Android 14+ | P1（中期） | Gradle |
| **iOS** | iOS 13.0 | iOS 17+ | P1（中期） | Xcode |

### 8.2 开发阶段规划

```
Phase 1 (MVP): Windows 桌面端
  ├── 核心功能：编辑、加密发布、解密阅读
  ├── 桌面特性：文件拖拽、窗口大小调整
  └── 目标：验证核心功能，收集用户反馈

Phase 2: Android 移动端
  ├── 适配移动端 UI（Material Design）
  ├── 移动端文件选择器适配
  ├── 触摸手势优化（编辑器交互）
  └── Google Play 上架准备

Phase 3: iOS 移动端
  ├── 适配移动端 UI（Cupertino 风格）
  ├── iOS 文件系统适配（Files App 集成）
  ├── iOS 加密合规审查
  └── App Store 上架准备

Phase 4: 三端统一优化
  ├── UI 一致性调整
  ├── 性能优化
  ├── 国际化支持
  └── 自动化 CI/CD
```

### 8.3 平台适配策略

#### 8.3.1 UI 适配

```dart
// 平台自适应组件示例
class PlatformAdaptiveButton extends StatelessWidget {
  final Widget child;
  final VoidCallback onPressed;

  @override
  Widget build(BuildContext context) {
    if (Platform.isIOS || Platform.isMacOS) {
      return CupertinoButton(
        onPressed: onPressed,
        child: child,
      );
    } else {
      return ElevatedButton(
        onPressed: onPressed,
        child: child,
      );
    }
  }
}
```

#### 8.3.2 文件操作适配

| 操作 | Windows | Android | iOS |
|------|---------|---------|-----|
| 文件选择 | 系统文件对话框 | 系统文件选择器 | Files App |
| 文件保存 | 系统保存对话框 | Downloads 目录 | Files App / 共享 |
| 拖拽导入 | 支持 (desktop_drop) | 不支持 | 不支持 |
| 文件关联 | .straw 文件关联 | Intent Filter | UTI 注册 |

#### 8.3.3 平台配置要点

**Windows:**
```cmake
# windows/CMakeLists.txt
# 关键配置
set(APP_NAME "StrawHut")
set(COMPANY_NAME "StrawHut Team")
# 需要 Visual Studio Build Tools
```

**Android:**
```xml
<!-- android/app/src/main/AndroidManifest.xml -->
<!-- 文件访问权限 -->
<uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
<uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
```

**iOS:**
```xml
<!-- ios/Runner/Info.plist -->
<!-- 文件访问描述 -->
<key>NSDocumentsFolderUsageDescription</key>
<string>StrawHut 需要访问文件以打开和保存 .straw 知识卡片</string>
```

### 8.4 构建与分发

| 平台 | 构建命令 | 输出格式 | 分发方式 |
|------|---------|---------|---------|
| Windows | `flutter build windows` | .exe / MSIX | 官网下载 / GitHub Releases |
| Android | `flutter build apk --release` | APK / App Bundle | Google Play / 官网下载 |
| iOS | `flutter build ios --release` | IPA | App Store |

---

## 9 P2P 扩展接口预留

### 9.1 设计原则

| 原则 | 说明 |
|------|------|
| **接口隔离** | P2P 功能通过独立接口定义，不影响核心本地功能 |
| **可选启用** | P2P 功能为可选插件，默认关闭 |
| **向后兼容** | P2P 升级不改变 `.straw` 文件格式 |
| **渐进式实现** | 先定义接口，再逐步实现具体 P2P 方案 |

### 9.2 P2P 接口定义

```dart
/// P2P 扩展接口（当前为空实现）
/// 未来可选接入 IPFS、BitTorrent 或自定义 P2P 网络
abstract class IP2PService {
  /// 发布知识卡片到 P2P 网络
  Future<P2PPublishResult> publishToNetwork({
    required StrawFile strawFile,
    required P2PPublishOptions options,
  });

  /// 从 P2P 网络发现/获取知识卡片
  Future<List<StrawFileMeta>> discoverCards({
    required P2PDiscoverOptions options,
  });

  /// 从 P2P 网络下载指定知识卡片
  Future<StrawFile> downloadCard({
    required String cardId,
    required P2PDownloadOptions options,
  });

  /// 检查 P2P 网络状态
  Future<P2PStatus> getStatus();

  /// 关闭 P2P 连接
  Future<void> shutdown();
}

/// P2P 发布结果
class P2PPublishResult {
  final String networkId;      // 网络中的唯一标识（如 IPFS CID）
  final List<String> seeders;  // 初始种子节点
  final DateTime publishedAt;

  const P2PPublishResult({
    required this.networkId,
    required this.seeders,
    required this.publishedAt,
  });
}

/// P2P 卡片元信息（用于发现列表展示）
class StrawFileMeta {
  final String cardId;
  final String title;
  final String publisherAlias;
  final DateTime publishDate;
  final List<String> tags;

  const StrawFileMeta({
    required this.cardId,
    required this.title,
    required this.publisherAlias,
    required this.publishDate,
    required this.tags,
  });
}

/// P2P 空实现（当前版本使用）
class P2PStub implements IP2PService {
  @override
  Future<P2PPublishResult> publishToNetwork({
    required StrawFile strawFile,
    required P2PPublishOptions options,
  }) async {
    throw UnsupportedError('P2P 功能尚未实现');
  }

  @override
  Future<List<StrawFileMeta>> discoverCards({
    required P2PDiscoverOptions options,
  }) async {
    throw UnsupportedError('P2P 功能尚未实现');
  }

  @override
  Future<StrawFile> downloadCard({
    required String cardId,
    required P2PDownloadOptions options,
  }) async {
    throw UnsupportedError('P2P 功能尚未实现');
  }

  @override
  Future<P2PStatus> getStatus() async {
    return const P2PStatus(
      isAvailable: false,
      message: 'P2P 功能尚未实现',
    );
  }

  @override
  Future<void> shutdown() async {
    // 空操作
  }
}
```

### 9.3 未来 P2P 方案评估

| 方案 | 适用场景 | 实现复杂度 | 与 StrawHut 适配度 |
|------|---------|-----------|-------------------|
| **IPFS** | 去中心化存储、抗审查 | 中等 | 高（已有 Dart 客户端） |
| **BitTorrent/WebTorrent** | 大文件分发、高并发 | 中等 | 中（需要种子管理） |
| **libp2p 自定义** | 完全定制化网络 | 高 | 高（但开发成本高） |
| **混合方案** | 渐进式升级 | 高 | 最高（推荐） |

**推荐路线：** 优先评估 IPFS 方案，因其成熟度高且有现成的 Dart 库可用。

### 9.4 接口集成位置

```
lib/
├── core/
│   └── p2p/                    # P2P 核心接口
│       ├── p2p_interface.dart  # IP2PService 接口定义
│       ├── p2p_models.dart     # P2P 数据模型
│       ├── p2p_stub.dart       # 空实现
│       └── (future)/
│           ├── ipfs_service.dart    # 未来 IPFS 实现
│           ├── bt_service.dart      # 未来 BT 实现
│           └── p2p_factory.dart     # P2P 服务工厂
```

---

## 10 非功能性需求设计

### 10.1 性能目标与实现策略

| 指标 | 目标值 | 实现策略 | 验证方法 |
|------|--------|---------|---------|
| **编辑器响应延迟** | < 50ms | Flutter 原生渲染、异步处理非关键操作 | Profiler 测量输入到渲染时间 |
| **文件加密导出时间** | < 3s (1MB 内容) | AES-256-GCM 硬件加速、异步加密 | 基准测试（1MB Delta JSON） |
| **文件解密渲染时间** | < 2s (1MB 内容) | 异步解密、渐进式渲染 | 基准测试（1MB 加密内容） |
| **应用启动时间** | < 2s (冷启动) | Flutter AOT 编译、延迟加载非关键组件 | 启动时间测量 |

### 10.2 性能优化策略

#### 10.2.1 编辑器性能

```dart
// 优化策略：
// 1. 使用 flutter_quill 原生渲染（无 WebView 开销）
// 2. 工具栏操作防抖处理
// 3. 大文档分块渲染
// 4. 图片懒加载

// 防抖示例
class DebouncedDraftSaver {
  Timer? _timer;
  final Duration _duration = const Duration(milliseconds: 300);

  void save(String content) {
    _timer?.cancel();
    _timer = Timer(_duration, () {
      DraftManager().saveToDraft(content);
    });
  }
}
```

#### 10.2.2 加密性能

```
优化策略：
1. 使用 AES-256-GCM（支持硬件加速的 AES-NI 指令集）
2. 加密操作在 Isolate 中执行（避免阻塞 UI 线程）
3. 大内容分块加密（每块 1MB）

Isolate 加密示例：
Future<EncryptedContent> encryptInIsolate({
  required String deltaJson,
  required Uint8List key,
}) async {
  return compute(
    _encryptWorker,
    _EncryptParams(deltaJson: deltaJson, key: key),
  );
}
```

#### 10.2.3 启动性能

```
优化策略：
1. Flutter AOT 预编译（Release 模式默认启用）
2. 延迟加载非关键页面（go_router lazy loading）
3. 首页最小化初始化（仅加载必要组件）
4. 预加载加密服务实例
```

### 10.3 可用性设计

| 需求 | 实现 |
|------|------|
| 中文界面（首期） | ARB 文件中文资源，默认语言中文 |
| 多语言预留 | flutter_localizations 框架，支持后续扩展 |
| 平台 UI 规范 | Material Design 3 (Android) / Cupertino (iOS) / Fluent 风格 (Windows) |
| 无障碍支持 | Semantic 标签、屏幕阅读器兼容 |
| 错误提示 | 明确的中文错误提示，引导用户操作 |

### 10.4 兼容性设计

| 维度 | 策略 |
|------|------|
| 文件格式版本 | format_version 字段 + 版本兼容处理逻辑 |
| 加密算法版本 | encryption_algorithm 字段标识，支持未来新增算法 |
| Flutter 版本 | 锁定 Flutter 3.x 主版本，定期升级评估 |
| 第三方依赖 | 锁定主版本，定期安全审计 |

---

## 11 错误处理与容错设计

### 11.1 错误分类

| 错误类型 | 场景 | 处理方式 |
|---------|------|---------|
| **用户操作错误** | 输入格式错误、文件选择取消 | 提示用户，允许重试 |
| **文件格式错误** | .straw 文件结构不完整、版本不兼容 | 明确错误信息，拒绝处理 |
| **加密错误** | 密钥错误、加密数据损坏 | 提示"密钥错误或文件已损坏" |
| **完整性校验失败** | 文件被篡改 | 提示"文件完整性校验失败，可能被篡改" |
| **文件系统错误** | 权限不足、磁盘空间不足 | 提示系统级错误信息 |
| **应用异常** | 未知运行时错误 | 通用错误提示，记录日志（仅本地） |

### 11.2 错误处理流程

```
错误发生
   │
   ▼
[捕获异常]
   │
   ▼
[异常分类]
   ├── 用户操作错误 → Snackbar 提示
   ├── 格式错误 → Dialog 提示详细信息
   ├── 加密错误 → Dialog 提示"密钥错误或文件已损坏"
   ├── 完整性校验失败 → Dialog 提示"文件可能被篡改"
   └── 未知异常 → Dialog 提示"发生未知错误"
   │
   ▼
[记录错误]（仅本地调试日志，不上传）
   │
   ▼
[恢复或退出]
   ├── 可恢复 → 回到上一状态
   └── 不可恢复 → 退出当前流程
```

### 11.3 全局异常处理

```dart
// Flutter 全局异常处理
void main() {
  // Flutter 框架异常
  FlutterError.onError = (FlutterErrorDetails details) {
    // 仅本地调试日志
    debugPrint('Flutter Error: ${details.exception}');
  };

  // Dart 异步异常
  PlatformDispatcher.instance.onError = (error, stack) {
    debugPrint('Platform Error: $error');
    return true;
  };

  runApp(const StrawHutApp());
}
```

---

## 12 架构约束与最佳实践

### 12.1 架构约束

| 约束 | 规则 | 原因 |
|------|------|------|
| **无网络请求** | 禁止引入任何 HTTP/WebSocket 相关依赖 | 产品定位为纯本地工具 |
| **无持久化存储** | 禁止使用 SQLite、SharedPreferences 等持久化方案 | 关闭后不留痕迹 |
| **不存储用户数据** | 不记录编辑历史、阅读历史、密钥记录 | 隐私保护要求 |
| **单例服务** | 核心服务采用单例模式 | 避免状态不一致 |
| **单向依赖** | 应用层 → 核心服务层 → 数据层，禁止逆向依赖 | 保持架构清晰 |

### 12.2 代码规范

```yaml
# analysis_options.yaml 核心配置
include: package:very_good_analysis/analysis_options.yaml

linter:
  rules:
    # 强制规则
    prefer_const_constructors: true
    prefer_final_fields: true
    avoid_print: true
    always_declare_return_types: true

    # 安全规则
    avoid_relative_lib_imports: true
    avoid_types_as_parameter_names: true

    # 性能规则
    prefer_const_declarations: true
    avoid_slow_async_io: true
```

### 12.3 安全最佳实践清单

| 实践 | 说明 | 检查点 |
|------|------|--------|
| 密钥不持久化 | 密钥仅在内存中处理，关闭应用后自动清除 | Code Review |
| 使用后清除 | 加密/解密完成后调用 clearSensitiveData() | Code Review |
| 输入验证 | 所有外部输入经过格式验证 | 单元测试覆盖 |
| 异常不泄露信息 | 错误提示不包含敏感信息（如完整密钥） | Code Review |
| 依赖安全审计 | 定期检查依赖包的安全公告 | CI/CD 流程 |
| 构建发布校验 | Release 构建后验证加密功能正确性 | 集成测试 |

### 12.4 架构演进指南

| 阶段 | 架构变化 | 触发条件 |
|------|---------|---------|
| v1.0 (当前) | 纯本地工具架构 | - |
| v1.x | 国际化支持、性能优化 | 多语言需求、性能反馈 |
| v2.0 | P2P 插件接入（可选） | 社区明确需求、技术验证通过 |
| v2.x | 新增内容类型（多媒体） | 用户需求驱动 |
| v3.0 | 协作编辑（可选） | 社区需求 + 技术成熟 |

---

> **文档声明**：本项目架构说明文档基于 StrawHut PRD v0.4.0 和 技术选型报告 v1.0.0 编制。架构设计将随着项目推进持续评估和优化。重大架构变更需经过评审后更新本文档。
