import 'package:flutter/material.dart';

/// 元数据预览组件
class MetaPreview extends StatelessWidget {
  const MetaPreview({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
