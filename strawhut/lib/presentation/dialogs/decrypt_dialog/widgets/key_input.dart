import 'package:flutter/material.dart';

/// 解密对话框 - 密钥输入组件
class KeyInput extends StatelessWidget {
  const KeyInput({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
